<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".room.MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Jetpacktuto"
        tools:targetApi="31">
        <activity
            android:name=".new_navigation.NavigationActivity"
            android:exported="true" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".old_navigation.FirstActivity"
            android:exported="true"/>
        <activity
            android:name=".joke.JokeActivity"
            android:exported="true"/>
        <activity
            android:name=".room.FormActivity"
            android:exported="true" />
        <activity
            android:name=".coroutines.CoroutinesActivity"
            android:exported="true" />
        <activity
            android:name=".RecyclerCountryActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>